export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Browserbase MCP Server</h1>
      <p>MCP server endpoint: <code>/api/mcp</code></p>
      <p>Health check: <code>/api/health</code></p>
    </main>
  );
}













